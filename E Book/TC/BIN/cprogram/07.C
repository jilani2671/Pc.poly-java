#include<stdio.h>
#include<conio.h>
void main()
int stdcall WinMain(HINSTANCE hInstance,HINSTANCE hPreInstance,LPSTRlpsz(mdline,int nCmdShow)
{
 HWND h;
 h=Create Window("Button","hit me",WSOVERLAPPEDWINDOW,10,10,150,0,0,i,0);
 Show window(h,nCmdShow);
 MessageBox(0,"hi!","waiting",MB_OK);
 return 0;
}